# 安全策略（SECURITY）

## 支持范围

| 版本 | 支持 |
|:--|:--|
| 最新发布（`master` 分支） | ✅ 接收安全报告 |
| 更早的历史提交 | ❌ 请先升级到最新 |

本仓库尚未正式发布版本（未打 tag）——当前 `master` 即支持版本。

## 怎么报告

**请不要在公开 issue 里贴漏洞细节**。用 GitHub 的私密通道：

1. 打开仓库页 → **Security** 标签 → **Report a vulnerability**（GitHub Security
   Advisories，私密，只有维护者可见）；
2. 若该入口不可用，开一个**不含细节**的 issue，说明"有安全问题，请开私密通道"，
   维护者会来联系。

我们会在确认后于 CHANGELOG 记录修复（按日期，不标版本号——本仓库的发布纪律）。

## 这个项目可能出什么问题（诚实范围）

这是一个**纯 Python、单进程、零第三方依赖**的逻辑判定引擎，没有服务端、不联网、
不读写用户文件（除你显式调用的 CLI 参数）。因此**攻击面很窄**，值得关注的是这几类：

| 类型 | 说明 |
|:--|:--|
| 拒绝服务（本地） | 指数级算法边界：真值表枚举、模型搜索、极小收缩枚举。构件已有**规模上限**并在超限时报 `size_limit` / `undecided`（诚实拦截而非硬跑）；若你发现某条路径能绕过上限把进程拖死，请报告 |
| 解析器健壮性 | 公式/λ 项/程序文本的解析器若在异常输入上抛未捕获异常（而不是返回 `*_pending`），属于缺陷，请报告 |
| 输出注入 | 引擎把**你的输入文本**回显进诊断报告（给人看）。若把输出直接渲染到 HTML/终端而不转义，风险在使用方一侧；本仓库只做纯文本输出 |
| 依赖与供应链 | 无第三方依赖、无 CI 密钥、无发布凭据；工作流只跑本仓库脚本 |
| 数据泄露 | 仓库与发布面有自动化检查（`engine/test_public_face.py`）拦本机路径、身份信息、私有名、密钥形态 |

## 不作为安全边界的东西

- 引擎输出是**诊断**，不是决策：`只诊断不决策`——不要把它接到会自动执行的动作上
  再期待安全保证；
- "算不动就诚实报告"（`undecided` / `size_limit`）是**设计行为**，不是漏洞；
- 本引擎不宣称绕过停机/哥德尔等定理（见 README 的边界声明）。

## 处理节奏

维护者是个人开发者（非专职），会在看到报告后尽快确认与修复；修复优先走
"补测试 → 修 → 回归全绿 → 记账"的常规流程。
